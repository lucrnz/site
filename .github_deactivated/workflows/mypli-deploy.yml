# name: mypli deploy

# on:
#   push:
#     branches: ["main"]

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Pull
#         run: |
#           curl -f -H 'KEY: ${{ secrets.MYPLI_KEY }}' '${{ secrets.MYPLI_URL }}/luxlibre/lucsite/pull'

#       - name: Deploy
#         run: |
#           curl -f -H 'KEY: ${{ secrets.MYPLI_KEY }}' -G '${{ secrets.MYPLI_URL }}/luxlibre/lucsite/deploy' -d 'capi=${{ secrets.CLOUDFLARE_API_TOKEN }}' -d 'pname=lucsite'
